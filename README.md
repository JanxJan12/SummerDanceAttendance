
  # Next Moves Company QR Attendance System

  A QR-based participant registration, scanning, and attendance monitoring system for Next Moves Company.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
